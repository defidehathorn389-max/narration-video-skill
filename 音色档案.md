# 音色档案：怎么在下次对话里复现同一个声音

## 一句话指令（你只要说这句）

> **「用 narration-video-skill 里的音色档案配音。」**

或者更短：**「用原来那个音色配音。」**

我看到这句就会去读本文件，按下面的参数重新注册音色、让你确认、然后开工。

---

## ⚠️ 先说清楚一个坑

`voice-00` 这个 ID **只在当前会话有效**。它是"本会话第几个注册的音色"这种序号，不是全局稳定的音色名。
**新开对话后 `voice-00` 不存在了**，就算存在也可能指向另一个声音。

所以复现靠的不是 ID，而是下面这套**参数 + 参考音频 + 客观特征**三件套。

---

## 音色档案

| 项目 | 值 |
|---|---|
| 本会话 voice_id | `voice-00` |
| 语言 | `zh`（中文普通话） |
| 注册方式 | `add_voice` 两个候选试听 → 用户投票选中 |
| 候选批次 | `index = 0`（默认那一对；同一 index 通常给出同一对候选，这是可复现的关键） |
| 用途标签 | `use_case = narration`（知识口播） |
| 性别参数 | 未指定（交由候选池决定） |

### 客观特征（本次实测，用于比对）

| 指标 | 实测值 |
|---|---|
| 中位基频 F0 | **156.9 Hz**（男女声交叠区，偏低；语调起伏大，范围 99–216 Hz） |
| 语速 | **4.72 字/秒 ≈ 283 字/分钟** |
| 电平 RMS | −20.2 dB（成片整轨均值 −23.1 dB / 峰值 −5.1 dB） |

### 参考音频（听这个比对，最可靠）

- `reference/音色样本.mp3` —— 成片旁白前 16 秒（**最常用，试听时放这个对照**）
- `reference/旁白完整轨.mp3` —— 完整 3 分 18 秒音轨
- 原始分段：`/home/user/audio/seg01.mp3 … seg10.mp3`

---

## 我在新会话里会怎么做（三步）

1. **注册**：用同一段试听文本 + `language="zh"` + `index=0` 调 `add_voice`，拿到两个候选
2. **请你比对**：两个候选各读同一段文本；你放着 `reference/音色样本.mp3` 对照，选**听起来一样**的那个
   - 顺带我会测一下候选中位 F0，接近 **157 Hz** 的优先
3. **开工**：拿到新的 `voice_id` 后按 SEGMENTS 分段合成，写入 `audio/seg*.mp3`

### 试听文本（固定用这段，约 15 秒 / 66 字）

```
你有没有过这种体验——手机拿起来，想看一眼时间，结果四十分钟过去了，你还在刷。
放下手机的那一刻，你甚至想不起来刚才到底看了什么。
```

固定文本的理由：同一段字才能比音色，而且它本身就是成片开头，等于顺带预览正片。

---

## 兜底：如果候选里没有那个声音

1. 先听 `index=0` 那一对 → 不像
2. 换 `index=1`、`index=2` … 每轮换一对新候选
3. 找到后我会把**新会话的 voice_id + 实测 F0** 回写进本文件，档案越用越准

> 现实预期：换会话后候选池可能变动，`index=0` 大概率还是原来那一对，但不保证。**所以每次都要你耳朵确认这一步**，别让我直接开始合成。

---

## 想换成别家 TTS

这套管线对 TTS 只有三个硬要求：中文自然、**能拿到每段精确时长**、能导出 mp3/wav。
满足就能直接替换，画面侧一行不用改（时长回写 `script.py` 的 `dur`，再跑 `build_audio.py`）。

- `edge-tts` —— 免费，中文自然度不错
- `CosyVoice` / `GPT-SoVITS` —— 开源可本地跑，支持**用参考音频克隆音色**（这才是真正"固定音色"的终极方案：拿 `reference/音色样本.mp3` 当参考，永远同一个声音）
- `ElevenLabs` / `Azure 语音` —— 商业方案，稳定

**如果你希望"永远同一个声音、再也不用投票确认"**，克隆类方案（CosyVoice / GPT-SoVITS）是唯一能做到的——本环境内置 TTS 不支持上传参考音频克隆。
