> 凭据前置要求：阅读SKILL.md开头的加密凭据规则。每次需要GitHub token时，先说明用途并主动向用户索取本次解密口令；不要读取聊天历史中的旧口令/token来绕过询问。未提供就停止凭据操作。默认密文路径：`/home/user/credentials/github-token.enc.json`。详细步骤见`docs/加密凭据与逐次询问规则.md`。

# 启动提示词（新对话里粘贴这段）

> 用法：新开对话 → 上传 `narration-video-skill.zip` → 把下面「完整版」整段粘进去 → 把 `<这里>` 换成你的稿子 → 发送。

---

## 完整版（推荐，复制这一整段）

```
我上传了 narration-video-skill.zip。请先解压到 /home/user/narration-video-skill/，
然后完整读一遍里面的 SKILL.md，严格按那套流程把下面的口播稿做成视频。

口播稿：
<把稿子整段粘在这里>

要求：
1. 音色 —— 读「音色档案.md」：用里面固定的试听文本（66 字那段）+ language="zh" + index=0
   调 add_voice，出两个候选让我听，我再对照 reference/音色样本.mp3 确认。确认前不要开始合成。
   候选实测中位 F0 接近 157 Hz 的优先。
2. 时间轴 —— 必须用每段 TTS 音频的实测时长回写 script.py 的 dur 字段，禁止按字数估算语速。
3. 底图 —— 用 SKILL.md §4.2 的提示词前缀（暖米白 #F3EEE5 + 四色限定），图上禁止出现任何文字。
   先出前 10 张拼成样张 contact sheet 给我看，我确认风格后再继续生成剩下的。
4. 版面 —— 上方 1920×900 画窗 + 下方 180px 纯色纸带放字幕，字幕和插画不得重叠。
   画面大字用留白探测自动落位，同一个词全片最多 2 次、间隔 ≥15 秒。
5. 成片 —— 1920×1080 / 25fps / MP4，h264 + aac，挂旁白音轨。
6. 编码必须分段（约 900 帧一段、切在镜头边界），否则 2GB 容器里 ffmpeg 会在第 512 帧被杀。
   ffmpeg 一律用 imageio_ffmpeg.get_ffmpeg_exe()，PATH 里没有裸 ffmpeg。
7. 做完按 SKILL.md §9 的 11 项阈值出一份质检数值报告给我。
```

---

## 精简版（赶时间用）

```
按上传的 narration-video-skill 里的 SKILL.md 做视频，音色按音色档案.md 复现（先让我确认），
口播稿如下：<稿子>
```

---

## 只做其中一段 / 只改一处

| 需求 | 说这句 |
|---|---|
| 只换音色重配 | 「按音色档案重新配音，画面不动」 |
| 只改某几句台词 | 「改 script.py 的这几句，重渲受影响的那几镜」 |
| 重新生成某张图 | 「重画 art/12_threshold.png，提示词要……」 |
| 补多状态帧 | 「给 X 镜头加 2–3 个状态帧」 |
| 只出分镜表 | 「先出分镜表给我看，别生图」 |
| 调节奏 | 「整体快/慢 10%」（改 LEAD/GAP/TAIL 或分段时长） |

---

## 三件事别让我自作主张

1. **音色**：必须你耳朵确认过再合成（候选池可能变，我不能替你选）
2. **画风**：前 10 张样张必须过目（风格一错，27 张全废，且生图有 10 张/轮上限）
3. **分镜表**：先出表再生图（表是后面所有步骤的合同）
