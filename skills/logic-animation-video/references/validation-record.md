# 打包时验证记录

整理日期：2026-09-16。

## 已完成

1. `scripts/check_rope_logic.py`：通过。验证累计时间模型单调、第一根在非中点相遇、第二根在总计 45 分钟时烧尽。
2. `python -m py_compile scripts/*.py`：通过。
3. 可移植脚本 `render_rope_demo.py --check`：通过，正确读取随包人物与 8 段 WAV，生成分镜和约 122.148 秒时间线。
4. 可移植脚本 `render_rope_demo.py --draft`：完整渲染通过。输出 960×540、15 fps、约 122.133 秒；音视频全片解码、H.264、AAC、yuv420p、faststart 检查通过。
5. 原始横版成片：1920×1080、30 fps、约 122.133 秒，7,919,624 字节；全片解码与格式检查通过。

## 未夸大为通过的项目

- 没有在第二台操作系统上执行；不同系统需配置中文字体。
- 没有声称字幕逐字强制对齐或完成新的听感验收。
- 没有量化测量参考视频与本例的每秒动画数量，不能用技术解码结果证明“动画密度超过参考”。
- 可移植包装脚本已验证完整草稿及关键帧；原始全分辨率脚本的成片已验证，没有在打包后重复做一次完整 1080p 渲染。
- 尚未上传 GitHub：缺少明确目标仓库地址与安全授权。

此文件记录打包时事实，不替代以后每次新作品的检查。
