# Skill：口播稿 → 知识类动画短片（成片 MP4）

## 凭据使用前必须询问用户（所有后续助手/模型必读）

用户已选择加密保存GitHub token。**每次需要使用凭据，先说明本次用途并主动索取本次解密口令，等待用户回复。即使聊天历史中存在旧口令或明文token，也不得复用来跳过询问。** 未提供则停止联网操作；解密失败不猜测，不回退到明文token。

密文默认位于`/home/user/credentials/github-token.enc.json`，口令不在Skill中。完整规则见[加密凭据与逐次询问规则](docs/加密凭据与逐次询问规则.md)，工具见`scripts/credential_vault.py`。只在本次授权的明确批次内使用，结束即退出；换任务、换会话或换模型都重新询问。缺少密文时询问其位置，不索要明文token绕过流程。文件加密不能消除既有聊天/工具记录中的暴露。

> 输入：一段中文口播稿（只有文字，没有画面、没有素材）
> 输出：16:9 MP4，配 TTS 旁白、AI 插画、运镜、逐字动画字幕、画面大字
> 参考成品：`/home/user/dopamine.mp4`（3 分 18 秒，1920×1080，25fps，31.4 MB）
> 参考代码：`scripts/`（script.py / build_audio.py / compose.py，可直接改稿复用）

这套流程的核心判断只有一条：**口播稿是唯一的事实来源**。
画面不"配"文字，而是把每一句台词里那个**机制**翻译成一张能被看懂的图；字幕、大字、时间轴全部由口播的真实音频时长反推，不靠估。

---

## 最新实测补充：音色、变速、多图与双端备份

开工前同时阅读 [音色-变速-多图-双端备份](docs/音色-变速-多图-双端备份.md)。
核心新增规则：用户重新选音色后核对实际音频版本；独立插画数不等于运镜段数；
变速先用 `atempo` 处理并实测，再重建时间轴；素材默认私有 GitHub + MCP 双端校验备份。
当前线粒体项目为1.2倍，变速工具见 `scripts/retime_audio.py`，不覆盖原速分段。
平台额度、字体、网络、磁盘等以当前环境实测为准，旧文档环境假设不是通用保证。

## 当前项目修订：结尾发音与1.2倍速

阅读 [固定结尾「脑子」与1.2倍速](docs/结尾脑子发音与1.2倍速.md)。
线粒体项目当前速度已改为1.2倍；「脑子」按nǎo zi列为每次必回听项。
用户确认错读的旧片段必须替换，ASR识别正确不能覆盖用户听觉反馈。
历史1.25倍文件仅作旧版本保留，不作为当前交付配置。

## 全片实施补充

阅读 [48图全片动画落地与状态帧验收](docs/全片动画落地与状态帧验收.md)。重点：干净底板与完整连通主体、状态帧语义淘汰、停顿插入后的全局时间平移、跨镜大字词的归属检查、封面/布局纳入缓存签名。

## 动画与封面硬性合规检查

必读 [动画与封面逐项验收](docs/动画与封面逐项验收.md)。固定顶部标题、单向整图推近、ASS着色字幕不能替代本Skill的大字、零件动画和四层字幕。必须审计实际执行的渲染器。

## 动画数据契约检查

发布前运行`scripts/validate_animation_contract.py`，传入当前storyboard和layout。检查字锚点、跨镜词、限次/间隔、落点与过程状态计划；它不代替看成片或人耳回听。当前线粒体项目传`--expected-images 48 --min-state-scenes 3`。

## 发布前强制检查入口

阅读 [图片密度、语速、TTS发音、音画对齐与成片验收](docs/密度-语速-音画对齐-验收规范.md)。
按项目输出PASS/WARN/FAIL/NOT_RUN及证据，不能把规范当作检查结果。
默认独立图目标8–12张/分钟；倍率服从用户；变速后实测并对齐。
独立ASR是发音辅助检查，不等于人耳通过；台词与每张图逐镜核对。

## 0. 全流程一览

| # | 步骤 | 产物 | 关键决策 | 详见 | 耗时 |
|---|---|---|---|---|---|
| 1 | 拆稿分段 | `script.py` 的 SEGMENTS | 按语义切 8–12 段，每段 10–35 s | §1.1 | 10 min |
| 2 | TTS 配音 | `audio/seg*.mp3` | 先试听选音色，再分段合成 | §2 | 15 min |
| 3 | 实测时长 → 时间轴 | `SEGMENTS[].dur` | **用音频实测，绝不估字数** | §2.4 §3 | 2 min |
| 4 | 分镜 | `分镜表.md` | 每 5–9 s 一镜，一镜一事 | §1.2 §1.3 | 20 min |
| 5 | AI 插画底图 | `art/NN_scene.png` | 统一提示词前缀 + 三不原则 | §4 | 3 轮 ×10 张 |
| 6 | 多状态帧（可选） | `art/10b_*.png` | 讲"过程"的镜头才加 | §4.6 | 1 轮 |
| 7 | 合成版面 | `compose.py` | 画窗 + 纸带 + 运镜 + 粒子 | §5 | 代码已就绪 |
| 8 | 字幕动画 | 逐字浮现 + 高亮 | 由 KEYS 驱动 | §6 | 代码已就绪 |
| 9 | 画面大字 | 22 处关键词 | 留白探测 + 限次打散 | §7 | 代码已就绪 |
| 10 | 编码输出 | `dopamine.mp4` | **必须分段编码** | §8 | 5 min |
| 11 | 数值质检 | 检查报告 | 11 项阈值 | §9 | 10 min |

**时间轴常量**（`script.py` 顶部）：

```python
LEAD = 1.6      # 片头留白（给标题/钩子一点呼吸）
GAP  = 0.34     # 段间停顿
TAIL = 2.2      # 片尾留白
```

---

## 1. 拆口播稿：分段与分镜

### 1.1 分段（Segment）

把整篇稿切成 **8–12 段**，每段 10–35 秒。切的刀口是**语义转折**，不是句子长度：

- 一段 = 一个完整的论证单元（提出问题 / 给出机制 / 举例子 / 转折 / 给方法）
- 段首常常是"但""所以""你有没有过"这类转折词——**它们是天然的切点**
- 太长的段（>35 s）TTS 容易后半段平淡，且一旦重录成本高

本项目 10 段的时长实测：`20.24 / 13.92 / 9.72 / 18.64 / 14.28 / 12.60 / 27.76 / 18.56 / 33.36 / 22.16`

### 1.2 分镜（Shot）

每段再切 2–3 个"镜"（`parts`），每个镜配 1–3 句字幕：

```python
{"dur": 18.64, "parts": [
    {"scene": "redot",  "lines": ["每一次你解锁手机，看到那个小红点，", "多巴胺就开始工作。"]},
    {"scene": "maybe",  "lines": ["它不给你满足，它给你的是"万一呢"。"]},
    {"scene": "maybe3", "lines": ["万一这条消息是好消息呢，", "万一这个视频很好笑呢，", "万一下面那个更好看呢。"]},
]}
```

**分镜的硬规则：**

1. **一镜只讲一件事。** 一句台词里出现两个机制，就切两镜。
2. **出现新概念 → 给新镜。** "多巴胺""阈值""前额叶""载体"这些词第一次出现时，都单独占一镜，因为观众需要时间建立画面。
3. **画面要"演示"机制，不要"装饰"文字。** 说"阈值被调高"，就画一个台阶在升高；说"前额叶说该睡了"，就画一个天平。不要画"一个人在玩手机"这种万能图。
4. **镜头时长 5–9 秒。** 短于 5 s 观众来不及读图，长于 9 s 运镜会显得呆。本项目 27 镜，平均 7.4 s。
5. **场景名 `scene` 用英文小写**，它同时是底图文件名（`04_redot.png`）和字典 key，改起来要全局同步。

### 1.3 分镜表长什么样

见同目录 `分镜表-样例.md`。格式：

```
| 镜号 | 时间 | 台词 | 画面 | 关键词 | 运镜 |
```

**先写分镜表、再生图**——这张表是后面所有步骤的合同。生图时对照它，才不会画出和台词无关的漂亮废图。

---

## 2. TTS：用什么、怎么用

### 2.1 音色选择

本环境用内置 TTS：先 `add_voice` 拿两个候选音色**说同一段你稿子里的真词**（不是"测试123"），让用户试听投票，拿到 `voice_id`（本项目 = `voice-00`）。

> **想在下一次对话里复现同一个声音？** 见同目录 **`音色档案.md`**——
> `voice_id` 只在当前会话有效，换对话就失效，所以那里记了固定的试听文本、`add_voice` 参数、
> 实测声学特征（F0 157 Hz / 语速 283 字每分）和参考音频 `reference/音色样本.mp3`。
> 用户只要说「用音色档案配音」即可。

试听文本的选择很关键：**直接用口播稿的前 30–45 个字**。这样它既是音色试听，也是成片的第一印象预演。

> 外部替代方案：ElevenLabs / Azure TTS / edge-tts / CosyVoice 都能用，**但必须满足一个条件——能拿到每段音频的精确时长。** 本流程的时间轴完全建立在实测时长上。

### 2.2 分段合成，不要一口气合成

```python
generate_speech(file_path="audio/seg01.mp3", text=<第 1 段>, voice_id="voice-00")
# 每段 ≤ 1500 字符；10 段各存一个文件
```

为什么分段：

- 某一段读错了，只需重录那一段
- **每段一个文件 = 每段一个精确时长**，这是时间轴的输入
- 并行合成，快

### 2.3 拼接音轨（`build_audio.py`）

```
seg01.wav + 0.34s 静音 + seg02.wav + … + 片头 1.6s 静音 + 片尾 2.2s 静音 → narration.wav
```

```bash
python3 build_audio.py        # 必须在 compose.py full 之前跑
```

音量目标：**均值 −23 dB / 峰值 −5 dB**（本项目实测 −23.1 / −5.1）。这个区间在手机外放和耳机里都舒服，不炸也不飘。

> ⚠️ 本环境**没有**裸 `ffmpeg`（PATH 里找不到，exit 127）。一律用 `imageio_ffmpeg.get_ffmpeg_exe()` 拿路径。

### 2.4 把时长写回 script.py

```python
with wave.open(w) as wf: 时长 = 帧数 / 采样率
```

**这是整套流程里最容易偷懒、也最致命的一步。** 用字数 × 语速估算，误差会累积到 5–10 秒，字幕和口播会明显错位。实测写入后，画面切点能精确贴在每个换气口上。

---

## 3. 时间轴：script.py 在算什么

`build_timeline()` 返回 `(shots, caps, total)`：

1. 从 `t = LEAD` 开始
2. 每个 part 占一段时长，**段内各句按"字数权重"分摊**（`weight()` 函数按字符加权，标点算 0.3 个字）
3. 段末 `t += GAP`

```python
def weight(s):                      # 每句话的"发声成本"
    return sum(1.0 if 是汉字 else 0.3 for ch in s)
```

结果：27 镜 / 51 条字幕 / 198.44 s。字幕之间**没有任何超过 0.5 s 的空档**——因为停顿只在段与段之间。

`compose.py` 直接 import 它，所以**改台词只需要改 script.py 一处**，画面时间轴自动跟着动。

---

## 4. AI 插画底图：用什么模型、什么提示词

### 4.1 模型

本环境：`generate_image`（内置图像模型，无法指定具体型号）。

**硬限制：每轮对话最多 10 张。** 27 张底图 = 3 轮。所以批量策略是：

1. **第 1 轮 10 张 → 拼成样张（contact sheet）送审**，风格过了再继续
2. 第 2、3 轮补齐剩余底图
3. 需要状态帧的话再加 1 轮

> 外部替代：Midjourney / Flux / SDXL 都能出这个风格，**同一套提示词前缀直接可用**。

### 4.2 提示词模板（逐字照抄）

```
Flat vector editorial illustration, warm off-white background #F3EEE5, limited palette
of deep ink navy #23201C, warm terracotta red #E4572E, muted gold #E9A83B, soft slate
blue #2E4A7D, clean geometric shapes, bold simple forms, subtle paper grain texture,
generous negative space, minimal infographic style, crisp edges, subject centered with
wide margins. Subject: <这一镜画什么>. No text, no letters, no words, no numbers, no watermark.
```

四个部分各自的作用：

| 片段 | 作用 |
|---|---|
| `Flat vector editorial illustration` | 定死风格：扁平矢量 + 编辑插画（不是 3D、不是写实） |
| `warm off-white background #F3EEE5` + 4 色限定 | **所有图共用一个调色盘**，拼在一起才像一部片子 |
| `generous negative space, subject centered with wide margins` | **给后面的大字留位置**——这一步决定了第七步（画面大字）能不能成功 |
| `No text, no letters, no numbers, no watermark` | **三不原则**：图上绝不能有字 |

**三不原则为什么是硬约束：** 文字/数字/刻度由代码画（中文字体渲染可控、可动画、可高亮），一旦烧进图里就成了死像素，改一个字要重生整张图，而且 AI 出的中文十有八九是错字。

### 4.3 `<这一镜画什么>` 怎么写

用**名词 + 关系**，不要用抽象概念：

- ✅ "a staircase with each step taller than the last, a small figure climbing, the top step glowing red"（阈值升高）
- ✅ "a balanced rope in static equilibrium, a calm slate blue block on the left end and a glowing terracotta spark on the right end"（前额叶 vs 多巴胺）
- ❌ "the concept of dopamine"（模型只能给你一团模糊的光）
- ❌ "a person addicted to phone"（万能图，没有信息量）

### 4.4 内容安全：会被拒的写法

**事故：** "two abstract opposing forces pulling a rope" → 被内容审核拒绝（判定为冲突/挣扎）。

**改写：** 去掉一切暗示对抗、暴力、挣扎的动词，改成静态构图：

```
a balanced rope with ... a calm slate blue geometric block on the left end and a
glowing terracotta red energetic spark-like shape on the right end ... static
equilibrium composition
```

**规则：** 用 `balanced / calm / static equilibrium / side by side` 代替 `fight / struggle / pull / attack / war / battle`。

### 4.5 底图质检

生完图先跑一次一致性检查，不合格的当场重生：

```python
# 四角取色，应全部 ≈ (243,238,229)
for f in art/*.png: 取 (3,3) (w-4,3) (3,h-4) (w-4,h-4) 四点均值
```

本项目实测：R 均值 241.9 / G 235.9 / B 224.2，**极差 50–72**（有几张带深色边框，可接受）。如果某张四角明显偏离（比如整张偏蓝），重生。

### 4.6 命名规则与多状态帧

```
art/01_hook.png        ← 数字前缀只为排序，场景名 = 第一个下划线之后的全部
art/10_maybe3.png
art/10b_maybe3.png     ← 同一场景的第 2 个状态帧
art/10c_maybe3.png     ← 第 3 个
```

`load_plates()` 按**第一个下划线后的名字归组**，所以 `10 / 10b / 10c` 会自动聚成同一个场景的 3 个状态，`compose.py` 会把镜头时长三等分、在每段末尾 28% 处交叉溶解到下一个状态。

**什么时候值得加状态帧（中等动效档的核心）：** 当这一镜讲的是一个**过程**而不是一个物件。

| 场景 | 状态帧 |
|---|---|
| 老虎机 maybe3 | 转动中 → 停两列 → 停三列（本项目已做） |
| 时钟 hook | 早上 → 深夜 |
| 阈值 threshold | 阈值低 → 阈值高 |
| 二倍速 movie2x | 1× → 2× |
| 缝隙 gap | 缝隙小 → 缝隙大 |

成本：每个多加 1–3 张图。**上限 10 张/轮**，规划好批次。

---

## 5. 合成版面：画窗 + 纸带 + 运镜

### 5.1 版面：画窗 + 纸带

```
┌────────────────────────────────────┐
│                                    │
│   画窗 1920×900（2.13:1 电影感）    │  ← 插画 + 运镜 + 粒子 + 大字
│                                    │
├────────────────────────────────────┤ ← 上沿 46px 羽化，插画淡出到纸面
│   纸带 180px：纯色 #F3EEE5 + 纸纹   │  ← 字幕专属区
└────────────────────────────────────┘
```

**为什么这么切：** 第一版用"全屏插画 + 底部半透明渐变蒙版"，蒙版在字幕位置只有 18% 不透明度，而有的图把 25–44% 的内容堆在画面下方——字幕必然和插画糊在一起。**现在纸带是完全不透明的纯色，物理隔离，永不重叠。**

### 5.2 底图自适应取窗

画窗是 2.13:1，底图是 1.83:1，必须裁掉 14% 的高度。不能简单居中裁（会切到脑袋）：

```python
def _best_window(im, nh):
    # 纵向滑窗打分：保留墨量最多 − 主体偏离中心的惩罚
    score = keep_ratio - 3.0 * max(0, |主体重心 - 0.48| - 0.12) ** 2
```

实测：**主体核心带（墨量 5%–95% 分位）保留率中位 100%、最低 96.9%**，27 张全部安全。

### 5.3 运镜（Ken Burns）

四种交替，按镜号取模，**全片不会是一个节奏**：

```python
kind = idx % 4
0: 1.0 → 1.12 缓推          1: 1.12 → 1.0 缓拉
2: 推 + 从右向左移           3: 推 + 从左向右移
e = u*u*(3-2*u)   # smoothstep，首尾无速度突变
```

推拉幅度 1.12 倍：够看出在动，又不至于让观众晕。0.4 s 内画面变化实测 1.2–1.4%。

### 5.4 氛围动效

22 个粒子在画窗内漂浮（金 / 暖白两种色），正弦漂移 + 匀速上升，透明度呼吸。**只在画窗内活动，不会飘进字幕带。**

镜头之间 **0.42 s 交叉溶解**，`smoothstep` 插值。切点必须落在镜头边界上——后面分段编码也依赖这一点。

### 5.5 动画：零件逐个入场 + 多状态帧（**让静态插画「活」起来**）

只有推拉运镜，观众仍会觉得是「一张会放大的图」。

> **⚠️ 但千万别用「整块主体呼吸 + 旋转」。**
> 我第一版就是这么做的：把主体抠出来单独放大缩小（±9.5%）、整体旋转（±2.2°）。用户反馈：**「看起来怪怪的」**。
> 原因：整块主体当成一个整体在飘，视觉上就是一张贴纸在呼吸和晃，而不是「机制在动」。运动量指标涨了 65%，观感反而更差。
> 教训：**指标好看 ≠ 观感对**。涉及审美一定要出样片给人看，别自己对着数字下结论。

真正有效的是这两条，都用上：

#### 路线 A：零件逐个入场（motion-graphics 式，纯代码，0 张新图）

把每张图拆成若干零件，按**左→右**依次滑入 + 淡入，1.2 秒内全部到位，之后**静止不动**。机制图基本都是「因果从左到右」，这个顺序刚好跟着叙事走。

| 参数 | 值 | 作用 |
|---|---|---|
| `REVEAL_DELAY` | 0.08s | 第一个零件的入场时刻（相对镜头起点） |
| `REVEAL_STAG` | 0.11s | 零件之间的错开间隔 |
| `REVEAL_DUR` | 0.42s | 单个零件入场时长 |
| `REVEAL_SLIDE` | 26px | 向上滑入的距离 |
| `REVEAL_SCALE` | 0.965 | 从 96.5% 放大到 100% |
| `MAX_LAYERS` | 6 | 一张图最多拆几个零件（阈值 0.06% 画面面积） |

缓动用 `smoothstep`：`e = a*a*(3-2*a)`。淡入的 alpha 要**按 1/24 量化后缓存** —— 每帧重算 alpha 太贵。

验证：镜头开头连续采样，画窗墨量应逐帧递增（`0 → 9.0万 → 13.4万 → 稳定`），确认是逐个进场而不是整块出现。

#### 路线 B：多状态帧（真·动画，要再生图）

同一个镜头生成 2–3 张**不同状态**的图，在镜头内依次交叉溶解。这才是「机制真的在动」：天平左倾→右倾、齿轮 0°→30°→60°、信号出发→半途→击中、分子释放→下行。

**命名即顺序**：`03_abuse.png` / `03b_abuse.png` / `03c_abuse.png` 自动归到同一场景 `abuse`，按文件名排序就是播放顺序。

切换逻辑（镜头内均分，每段后 38% 溶解到下一状态）：
```python
n = len(ps); hold = 1.0 / n
seg = min(int(u / hold), n - 1)
k = clamp(((u - seg * hold) / hold - 0.62) / 0.38) if seg < n - 1 else 0.0
img = ps[seg].frame(z, px, py, t, t_shot if seg == 0 else 1e3)
```

> **⚠️ 血泪：状态帧的 `t_shot` 不能跟着状态重置。**
> 我第一版写成 `t_shot = shot_dur * seg * hold`，结果第一个状态的 `t_shot = 0` → 零件永远不入场，**镜头前 1/3 是空白**。
> 正确：进场动画只在第一个状态播，后续状态直接完整显示。

#### 拆零件：先验「环」的纯净度

抠出主体后要**补洞**（主体移走的位置用纸色填），所以主体周围一圈必须是干净的纸：

```python
d = np.abs(a - BG_RGB).sum(axis=2)
mask = d > 42                                    # 非纸色 = 主体
mask = ndimage.binary_closing(mask); mask = ndimage.binary_opening(mask)
ring = dilate(mask, 6) & ~mask                   # 主体外扩 6px 的环
purity = 1 - mask_of(ring).mean()                # 环里纸色占比
# purity > 0.90 才能拆 —— 实测 27 张全部 100%
```

`ndimage.label` 取连通域，按面积取前 N 个；小碎块归到**中心最近**的那个零件，避免「有的动有的不动」。

> **⚠️ 血泪：零件掩膜必须严格互斥，否则就是重影。**
> 我第一版把最后一层的掩膜写成 `np.isin(lab, keep)`（包含**所有**层），结果 16/27 张图里前面几层的零件被**再画一遍**，而且最后一层是覆盖全图的大框，缩放+旋转后超出画面被切 —— 看起来就是「重影 + 组件截断」。
> 正确写法：每个 label 只能分给一个零件，`m = np.isin(lab, [lid for lid in ... if layer_of[lid] == k])`。
> **验证方法：** 把各层 alpha 贴回 base 坐标累加，`(acc > 1).sum()` 必须为 **0**。用 bbox 判断会误报（L 形零件的包围盒本来就会互相套住），必须查像素。

#### 取景不截断：给底图加纸边 + 自适应推近上限

AI 出图的主体往往占满画面（实测 **86%**），而取景窗也是 86% → 零余量，一推就切。实测 27 张全部有截断风险，最坏被切 1375px。两步解决：

**① `PAD = 1.18`：给底图加一圈纸边**（纸色 + 3 颗粒，与原图无缝）。主体占比降到 ~72%，运镜余量就出来了。附带好处：成片更锐（放大倍率 1.58× → 1.34×）、文件更小。

**② `z_fit`：按每张图算出「不切到零件的最大推近倍率」**
```python
gw = (bx1 - bx0) / 2 + 10        # 零件并集 bbox + 余量
z_fit = min(bw / (2 * gw), bh / (2 * gh))    # 取景窗必须装得下零件
z = min(z_kenburns, z_fit)
```
实测：加边后 25/27 张能满推（中位数 1.16），**截断从 42% 的取景降到 0**。

> **⚠️ 验证截断别用「画面边缘有没有墨」：** 暗角本身就压暗四边，会全篇误报。要检测**插画层本身**（不含暗角），而且要跟「整张图全显示」的基线比 —— 有的源图内容本身就贴边（比如火锅碗顶到画面底边），那不是你裁的。

#### AI 出状态帧的技巧与坑

- **用已经生成好的那张当参考图**去生成下一状态：`generate_image(images=[已有图], prompt="只改 X，其余不变")`。风格和构图能锁住，实测底色偏离 ≤5、墨框位移 ≤25px。
- 提示词要写死「只改 X，其余一切不变，不许移动 / 增删 / 缩放任何元素」。写「金线更长更亮、星星变大」会让 AI 重排构图 —— 实测漂移 **323px**；改成「只改颜色，形状尺寸一律不许动」后降到 **1px**。
- **有的图 AI 就是不肯改**：地图那张连试 3 次（填色 / 加圆点 / 加粗描边）都原样返回，差异 0.04%。别死磕，放弃这一镜的状态帧，不值得为它卡住出片。
- **每张状态帧都要机器验一遍**：差异像素、墨框位移、底色偏离。差异 <0.15% 等于白生成。
- **分辨率不一致会让检查失效**：比对前先把两张 resize 到同尺寸，否则「墨框位移」是拿不同尺度的坐标在比，数值全是假的。

#### 手持微晃（与上面叠加）

给运镜加双频正弦漂移，整帧持续游移（不是只有推拉）：
```python
dx = HAND_X * sin(t*1.10 + i*1.7) + HAND_X*0.6 * sin(t*1.97 + i*3.1)
```
注意 `px/py` 的范围是 **[-1,1] 对应整个可取景范围**，不是画面比例 —— 初版按画面比例给 0.0075，实际只有 1–2 px，等于没加。同时把起幅抬到 `ZMIN = 1.05`：z 越接近 1 可取景范围越窄，起幅 1.0 时**完全没有漂移空间**。

> **踩过的坑：** 扫光在米白底色上完全无效 —— 白光（255,255,255）和纸色（243,238,229）只差 12 个色阶，17% 透明度下变化仅 0.2%，白占 5 ms/帧。米白底别做扫光。

### 5.6 静态图层烘焙（性能关键）

暗角、中心柔光、纸带、纸纹**全部预先合成成一张 `finish` 图层**，每帧只做一次 `alpha_composite`。

> **事故：** 第一版每帧算一次半径 234 的高斯模糊做光晕 → **1.28 s/帧，全片要 106 分钟**。烘焙 + 换 BILINEAR 重采样 + rawvideo 直传后 → 0.086 s/帧。现在 0.051 s/帧。
> **规则：任何与帧号无关的图层，一律烘焙。绝不在帧循环里做大半径模糊。**

---

## 6. 字幕动画

### 6.1 断句

一句台词 = 一条字幕（由 `script.py` 的 `lines` 定义）。51 条，中位 18 字，最长 31 字，最短 1.74 s。

```python
size  = 56 if len(text) <= 28 else 50     # 保证一行放得下
lh    = size * 1.42                        # 行高
y0    = BAR_TOP + (BAR_H - 块高)/2 - size*0.30   # −0.30 是字形重心补偿
CAP_MAXW = 1600    TRACK = 3               # 最大宽 / 字距
```

实测 51 条字幕**墨迹重心平均 989（纸带中心 990），上下各留 30/50 px 余量，零越界**。

### 6.2 关键词高亮

```python
KEYS = ["多巴胺", "想要", "阈值", "万一", "前额叶", "延迟满足", "主导权", ...]  # 35 个
```

KEYS 怎么定：**通读全稿，把"机制的名词"全部挑出来**——观众需要记住的词。不是形容词，是"东西的名字"。

命中 KEYS 的字：**红色 #E4572E + 加粗**。

### 6.3 四层动画

| 动画 | 参数 | 效果 |
|---|---|---|
| 逐字浮现 | `REV_DUR=0.18`，打字时间占字幕时长 `REVEAL_FRAC=0.60`，按字数均分 | 字一个个淡入 + 上浮 14px，节奏贴着语速 |
| 关键词弹动 | `POP_DUR=0.44`，`easeBack` 回弹 | 词放大到 1.16 倍再落回 |
| 荧光笔块 | `CARD_DUR=0.30` | 金色圆角块从 55% 亮度落定到 15%，像马克笔划重点，之后一直托着这个词 |
| 打字光标 | 高 0.42 alpha 的红色竖条 | 跟随打字位置，打完即隐 |

```python
def ease_back(k):                       # 冲过一点再回来
    c1, c3 = 1.70158, 2.70158
    return 1 + c3*(k-1)**3 + c1*(k-1)**2
```

**性能：** 单字贴图缓存 `_GLYPH[char,size,hot]`，只有过渡中的 2–3 个字需要重算 alpha 蒙版，其余直接 paste。

---

## 7. 画面大字

这一层是"突显内容"的主力——在插画的留白处砸一个 68–124 px 的关键词。

### 7.1 选词

```python
for 每个镜头:
    找出该镜台词里命中的 KEYS，取最长的那个
    全片限次：同一个词最多 2 次，且间隔 ≥ 15 s
    时间窗：max(字幕起点, 镜头起点+0.5) → min(字幕终点+0.65, 镜头终点−0.1)
    窗口 < 0.95s 就放弃
```

本项目：27 镜里 22 镜有字，共 22 处。

> **不加限次会怎样：** 第一版"多巴胺"砸了 4 次、"万一"砸了 4 次，像弹窗广告。限次后最多 2 次。

### 7.2 自动找留白（这一步最关键）

给每张底图建一张 **64×30 的墨量密度图**，7 个候选区里挑最空的：

```python
cands = [左上, 右上, 左下, 右下, 上中, 左中, 右中]
score = 该区墨量密度
      + 1.2 if 密度 > 0.20                    # 真挤的地方基本不排
      + 0.55 if 上一个字也用了这个区           # 不连着用同区
      + 0.28 if 上上个也用了
      + 0.10 × 该区已用次数                   # 全局均衡
      + 0.10 × 同镜头已占位置的重叠数
```

> **不加打散惩罚会怎样：** 第一版 27 个字里 **22 个全挤在左上**（因为那儿最空）。加了惩罚后 7 个区各 3–4 次。

实测：落点密度最高 0.057，而底图整体密度中位 0.162——**每个字都落在比全图平均更空的地方**。

### 7.3 颜色语义

| 颜色 | 用在哪 |
|---|---|
| `#E4572E` 砖红 | 问题侧：多巴胺、劫持、阈值、赌局、算法、填不满… |
| `#2E4A7D` 板岩蓝 | 解方侧：前额叶、延迟满足、重新训练、主导权、出口… |
| `#23201C` 墨黑 | 中性：载体、机制… |

### 7.4 动画

```
弹入 0.52s：scale 0.86 →(easeBack)→ 1.0，冲到 1.07 再落回
飞入 30px：从离它最近的画框边缘飞进来
柔光垫底：字后一层模糊的纸色圆角块（50% alpha，模糊半径 16）
细线扫出：字下方 7px 细线横向扫出，颜色跟字走
淡出 0.26s
```

实测高度序列 `130 → 148 → 148 → 146`，回弹真实存在。

---

## 8. 编码输出（**必须分段**）

### 8.1 帧传输

```bash
ffmpeg -y -v error -f rawvideo -pix_fmt rgb24 -s 1920x1080 -r 25 -i - \
  -an -c:v libx264 -preset veryfast -crf 21 -threads 2 \
  -x264-params rc-lookahead=20:ref=3:bframes=3 -pix_fmt yuv420p 段.mp4
```

Python 端用 `subprocess.Popen(stdin=PIPE)`，**不落盘中间帧**（4961 帧 × 6.2 MB = 31 GB，落盘不可行）。

### 8.2 ⚠️ 事故：单行 ffmpeg 会在第 512 帧被杀

**现象：** 全片渲染连续两次死在第 512 帧，ffmpeg stderr **完全为空**，退出码 1。

**排查（依次排除）：**
| 假设 | 结论 |
|---|---|
| 管道坏了 | ✗ 喂 700 帧噪声数据正常完成 |
| 磁盘满了 | ✗ 剩余 20 GB |
| Python 内存泄漏 | ✗ 900 帧稳定在 409 MB，不涨 |
| **容器内存 2 GB，x264 编码真实画面时打满** | ✅ |

**为什么噪声数据测不出来：** 噪声帧压出来只有 1 MB，编码器几乎不占内存；真实画面 x264 的参考帧/前瞻缓冲才是吃内存的大户。

**解法：分段编码。**

```python
bounds = 在镜头切换点切分（~900 帧一段，绝不切断交叉溶解）
每段独立起一个 ffmpeg → 内存逐段清零
最后 concat demuxer 拼接（-c:v copy，不重编码）
```

本项目切成 5 段：`0–36.4s / 36.4–80.1 / 80.1–121.2 / 121.2–164.0 / 164.0–198.4`，每段独立成功后写盘，失败只重跑那一段。

```bash
ffmpeg -f concat -safe 0 -i list.txt -i 旁白.mp3 -c:v copy -c:a aac -b:a 192k -shortest -movflags +faststart out.mp4
```

### 8.3 性能对照

| 版本 | 单帧 | 全片 |
|---|---|---|
| v1（每帧高斯模糊 + PNG 传帧） | 1.28 s | 106 min |
| 烘焙静态层 + rawvideo + crf22 | 0.086 s | 7 min |
| 最终（veryfast + 画窗变小） | **0.051 s** | **4.7 min** |

---

## 9. 质检清单

成片必须过这些数值关（脚本见 `compose.py` 同级的验证片段）：

| 检查项 | 阈值 | 本项目实测 |
|---|---|---|
| 帧数 | = 时间轴 × fps | 4961 ✓ |
| 规格 | 1920×1080 / 25fps / h264+aac | ✓ |
| **纸带纯净度** | 非文字区 ≈ (243,238,229)，抖动 <5 | (240,234,225) ±0 ✓ |
| **字幕不越界** | 墨迹 y ≥ 902 | 963–1015 ✓ |
| **大字不污染字幕带** | 纸带区帧差 < 40 | 0 处 ✓ |
| 大字出现 | 框内变化量 > 对照区 1.6 倍 | 22/22 ✓ |
| 逐字动画 | 同条字幕内墨量递增 | 1457 → 11134 ✓ |
| 画面在动 | 相邻 0.4 s 画窗变化 >0.5% | 1.2–14% ✓ |
| 无黑帧/纯色帧 | 灰度标准差 >8 | 0 张异常 ✓ |
| 音量 | 均值 −23 dB / 峰值 −5 dB | −23.1 / −5.1 ✓ |
| 分段接缝 | 帧差落在正常转场区间 | 4.4–9.9%（对照 0.67–11.67%）✓ |

---

## 10. 常见坑（血泪清单）

1. **裸 `ffmpeg` 不存在** → `imageio_ffmpeg.get_ffmpeg_exe()`
2. **估语速定时间轴** → 必然错位，必须实测每段音频时长
3. **`generate_image` 每轮上限 10 张** → 提前规划批次，先出样张送审
4. **提示词里出现 fight/struggle/pull** → 内容审核拒绝，改 `balanced / static equilibrium`
5. **让 AI 在图里写字** → 错字 + 无法动画，字全部由代码画
6. **帧循环里做大半径模糊** → 1.28 s/帧，必须烘焙静态层
7. **长视频单行 ffmpeg** → 2 GB 容器下必死，必须分段编码
8. **字幕用半透明蒙版盖在插画上** → 必重叠，要用不透明纸带物理隔离
9. **大字不限制位置/词频** → 22 个全挤左上 + 同一个词砸 4 次
10. **`narration.wav` 被清理后没重跑 `build_audio.py`** → ffmpeg 找不到音轨直接断管（现在已改成找不到就警告并静音输出）

### 10.1 ⚠️ 两个会「静默丢产物」的环境坑（最容易白干）

**① 工作区快照有约 128 MB 上限，超了会静默丢弃最大的文件**

症状：上一轮明明渲好的成片，下一轮 `ls` 就没了 —— 不报错、不提示，直接消失。

实测：`/home/user` 到 164 MB 时，35 MB 的成片被丢（164 − 35 = 129 ≈ 上限）；更早一次 233 MB 时，31 MB 的 `narration.wav` 被丢。

对策（**开工前就做，别等丢了再救**）：

| 做法 | 说明 |
|---|---|
| 音轨用 **mp3 不用 wav** | 178 s 立体声：wav 31 MB → mp3 仅 3.4 MB，省 28 MB |
| `build_audio.py` 跑完立刻删中间文件 | `rm audio/seg*.wav narration.wav`（都是可再生的） |
| 上一个片子交付后归档/删除 | 29 张底图 + 成片 ≈ 74 MB，是最占地方的东西 |
| **每轮收尾前 `du -sh /home/user`** | > 100 MB 就要警惕，别把大文件留到跨轮 |
| 大产物渲染完立刻验证存在 | 别只 `present_file`，下一轮开头再 `ls` 一次 |

**② Python 包不跨消息保留**

沙箱只持久化 `/home/user` 的文件，`pip install` 装的东西会丢。开工第一步：

```bash
python3 -c "import numpy, PIL, imageio_ffmpeg" \
  || pip install --quiet numpy pillow imageio_ffmpeg
```

（scipy 可选；需要连通域抠图时 `pip install scipy`，它提供 `ndimage.label`）

---

### 10.2 ⚠️ 改代码时把文件写爆（900 行 → 238 万行 / 100 MB）

为了给 `frame()` 换实现，我写了：

```python
old = s[s.index("    def frame(self, z, px, py, t):") : s.index("    def _subject_box(self):")]
s = s.replace(old, new_frame)
```

问题是 `_subject_box` 定义在 `frame` **之前**，所以 `start > end` → 切片结果是**空字符串** → `s.replace("", 新代码)` 把新代码插进了**每一个字符之间**。文件从 39 KB 炸到 100 MB / 238 万行。

**规矩：**

1. 替换前必须 `assert s.count(old) == 1`，匹配 0 次或多次都要停下来看
2. 别用 `s.index(a):s.index(b)` 取片段 —— 先 `assert s.index(a) < s.index(b)`
3. **多个替换要等全部成功后再统一写盘**，中间任何一个断言失败就整体回滚（我靠这一点避免了半改状态的文件）

**真炸了也别慌，可以还原：** 原文字符没丢，只是被等长的片段隔开了。设插入片段长度为 L，则 `原文[i] = 损坏文件[L + i*(L+1)]`：

```python
data = open('compose.py', encoding='utf-8').read()
for L in range(1, 40000):                 # 找步长：data[L] 应当等于原文首字符
    if data[L] == '#':
        cand = data[L::L+1]
        if cand.startswith('# -*- coding: utf-8 -*-'):
            open('recovered.py','w').write(cand); break
```
实测：插入片段 2523 字符，原文 39017 字符完整还原。

### 10.3 TTS 长句尾部掉调（字调读错）

**症状：** 41.5 秒的第 10 段，最后一句「从看清自己**脑**子里那套系统开始」的「脑」音调不对（用户听出来的，我听不到）。

**原因：** TTS 在超长语段的尾部韵律会退化。

**解法：** 把收尾句**拆成独立的一段**重新合成（41.5s → 35.6s + 6.1s 两个短段）。附带好处：收尾句前面多一个换气停顿，节奏更好。

**拆法：** 在末尾找 >0.15s 的静音段（实测 35.17–36.15s 有 0.98s 静音），从中点下刀：

```bash
ffmpeg -y -i seg10_full.mp3 -t 35.55 -c copy audio/seg10.mp3
```

**TTS 有随机性，重配是有效手段：** 同一句文本连发 3 次，得到 6.10s / 5.38s / 5.69s 三个不同结果（md5 各异）。所以读错了就直接重生成，别改文本。

**验证字调要靠 F0：** 上声（第三声）在轻声前是**低降调**，F0 应从 ~150 Hz 降到 ~100 Hz。用自相关提 F0 看轨迹。但这个指标不稳（音节定位容易偏），**最终还是得人耳确认** —— 拿不准就多生成几个版本让用户挑。

## 11. 换一篇新稿子要改什么

| 文件 | 改什么 |
|---|---|
| `scripts/script.py` | `SEGMENTS`（分段 + 分镜 + 台词 + 实测时长）、`KEYS`（关键词表） |
| `art/` | 全部底图重生成（提示词前缀不变，`<Subject>` 按新分镜表写） |
| `compose.py` | `KEYS`、`RED_WORDS` / `SLATE_WORDS`（颜色语义）、必要时调 `ART_H` / `BAR_H` |
| 其余 | 版面、运镜、动画参数、编码策略——**全部不用动** |

### 命令

```bash
cd scripts/
python3 build_audio.py                                    # 拼接音轨（先做）
python3 compose.py preview                                # 每镜中点出一张样帧，检查构图
python3 compose.py test --start 0 --end 16 --out /tmp/t.mp4   # 试片
python3 compose.py full --fps 25                          # 成片（自动分段编码 + 挂音轨）
```

### 中文字体

`/usr/share/fonts/opentype/noto/NotoSansCJK-{Regular,Bold}.ttc`
**face index = 2 才是 SC**（简体中文），默认会取到日文/韩文字形。

---

## 12. 附：完整参数表

```python
# 版面
W, H      = 1920, 1080
ART_H     = 900          # 画窗高（纸带 180px）
ZOOM      = 1.12         # 运镜最大倍率
FADE      = 0.42         # 镜头交叉溶解

# 配色
BG_RGB = (243, 238, 229)   INK  = (35, 32, 28)
RED    = (228, 87, 46)     GOLD = (233, 168, 59)   SLATE = (46, 74, 125)

# 字幕
CAP_MAXW = 1600   TRACK = 3
REV_DUR = 0.18    POP_DUR = 0.44    CARD_DUR = 0.30
OUT_DUR = 0.20    REVEAL_FRAC = 0.60   SHOW_CARET = True

# 大字
BIG_PAD = 30   BIG_MARGIN = 148   BIG_TOP = 130   BIG_BOTTOM = 812
BIG_POP = 0.52   BIG_R = 1.12
字号：≤2字 124 / 3字 110 / 4字 96 / ≤6字 82 / 更长 68

# 编码
preset veryfast  crf 21  threads 2  rc-lookahead=20:ref=3:bframes=3
yuv420p  +faststart  aac 192k  分段 ~900 帧（切在镜头边界）
```

---

## 踩坑清单（必读）

开工前先读 [`docs/踩坑清单.md`](docs/踩坑清单.md)。里面是真实翻过车的记录，
每一条都有实测数据。最容易毁掉成品的三条：

1. **工作区快照约 128 MB 上限，超出会静默丢弃最大的文件（通常就是成片）。**
2. **`/tmp` 是 tmpfs（内存盘）——把文件挪过去不省空间，只会吃 RAM。**
   实测把 46 MB 底图移到 /tmp 导致内存耗尽、渲染速度掉到 1/10。
   省空间的正确做法是：推到私有 GitHub 仓库 → 验证远端完整 → 删本地。
3. **`generate_image` 和 `generate_speech` 各自每轮硬上限 10 次**，
   素材都是按轮限流攒出来的，一旦备好就该先推仓库备份。
